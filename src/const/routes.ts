export const HOME = 'Home';
export const ID_ROUTE = ':id';
export const LOGIN = 'Login';
export const LOGIN_ROUTE = '/';

export const HOME_ROUTE = `/${ID_ROUTE}`;

const exportedObject = {
  HOME_ROUTE,
  HOME,
  ID_ROUTE,
  LOGIN_ROUTE,
  LOGIN
};

export default exportedObject;
